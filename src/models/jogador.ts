/** Registro já normalizado, pronto para a saída. */
export type JogadorNormalizado = {
  club_id: string;
  player_id: string;
  name: string;
  age: string;
  goals: string;
  debut_date: string;
  position: string;
  shirt_number: string;
};
