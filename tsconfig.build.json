{
  // Mesmas regras do tsconfig.json, mas emitindo `dist/` a partir de `src/`.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src/**/*.ts"]
}
